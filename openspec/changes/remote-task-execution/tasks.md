# Tasks

## 1. 协议和存储

- [ ] 1.1 增加远程请求、能力、任务及事件模型与输入限额；验证中文参数、非法模式/目录/超时、未知字段及内容摘要测试。
- [ ] 1.2 追加模式 3 的角色分离表和迁移兼容逻辑；验证模式 2 升级、备份、请求冲突、控制端历史与 Agent 独占目录。

## 2. 执行端访问

- [ ] 2.1 实现本地执行端身份、TLS 证书、Tailscale 地址绑定和后台进程生命周期；验证默认无监听、错误地址拒绝、UI 关闭后服务保持运行。
- [ ] 2.2 实现一次性配对、控制端令牌摘要、任务所有权和撤销；通过过期/重放/错误次数/越权集成测试。
- [ ] 2.3 接入三平台凭证存储与证书固定客户端；验证凭证库失败不降级、证书变化和重定向拒绝。

## 3. 执行和结果

- [ ] 3.1 实现持久队列、exec/script 执行和环境/目录校验；用合成进程验证输出、退出码、幂等、队列限额和并发。
- [ ] 3.2 实现 Windows Job 与 POSIX 进程组、取消和超时；验证普通父子进程收束与自然退出竞态，未具备的平台不标记实测。
- [ ] 3.3 实现分页输出、事件、合法自报进度、输出限额与未知恢复；验证输出洪泛、UTF-8 边界、断线补齐和故障不重放。
- [ ] 3.4 实现限定结果目录、普通文件快照、manifest 和有所有权校验的 Range 下载；验证路径穿越/symlink/超限/摘要失败与 .partial 恢复。

## 4. 桌面集成

- [ ] 4.1 增加本机执行端管理、目录选择、配对信息和授权撤销界面；验证真实开启、失败提示与独立生命周期。
- [ ] 4.2 增加设备配对及目标明确的命令/脚本表单；验证网络切换、提交未知、重复点击和控制端不误执行命令。
- [ ] 4.3 增加远程任务历史、日志/事件/参数/结果详情和取消；验证重开恢复、离线不误报失败、日志按文本显示和键盘操作。
- [ ] 4.4 接入各平台登录后自启适配与执行端打包；验证注册/停止失败反馈，保持新安装默认关闭。

## 5. 验证和发布

- [ ] 5.1 完成 Rust/前端全项目检查、TLS 与进程集成测试；记录真实命令和结果，修复失败项。
- [ ] 5.2 完成 Windows 原生安装与端到端配对执行、任务取消和重启测试；其他两类系统分别验证 3×3 跨平台组合，环境缺失保留未验证。
- [ ] 5.3 更新 GitHub Issues、需求与验证说明，生成三平台安装包及校验和；发布版本能力必须与实现一致。
