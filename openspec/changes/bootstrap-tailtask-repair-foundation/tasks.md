# Tasks

本清单对应本次基础里程碑，验收契约见四份 specs，技术选择见 design.md。只有已有证据的准备事项标为完成；代码存在不等于验收通过。发布凭证或平台环境缺失只阻塞对应交付步骤，不阻塞其他本地实现。

## 1. 已完成的设计与工程准备

- [x] 1.1 按用户选择写入 AGENTS.md、GitHub Issues 和单一领域上下文说明；通过读取三个配置文件确认内容与审阅草稿一致，无 triage 标签占位文件。
- [x] 1.2 用内置 GPT Image 生成并修正简约科技 UI 概念图，保存图片和两轮提示词；人工检查本机任务目标一致且图中含“概念设计 · 示例数据”说明。

## 2. 构建基线和范围文档

- [x] 2.1 修复工作区 SQLx 迁移宏功能配置并清理相关编译警告；运行 `cargo test -p tailtask-core --locked`，确认原有 Rust 测试实际通过。
- [x] 2.2 补齐 Tauri 所需 png/ico/icns 图标，确保配置路径对应真实文件；运行前端生产构建与桌面编译，验证图标资源和主窗口权限清单可用。
- [ ] 2.3 固定可构建 Node/Rust 工具链与依赖锁文件，核对 rust-version 声明；在锁定依赖下完成 `npm ci`、`npm run check`、`npm test`、`cargo check --workspace --locked`，记录实际支持版本。
- [x] 2.4 更新 README、权限边界与 app_info 中过时的 UI 状态，增加对原 PRD 的本里程碑范围说明；核对文案明确“简约科技风格已选定、本机限定修复、远程未开放”，不将基础版称为完整远程任务产品。

## 3. 本地数据与可靠修复执行

- [x] 3.1 在 core 增加自有数据库识别及模式版本检查，兼容旧骨架数据库并追加迁移保存产品标记/目标范围；用新库、原始模式库、无关 SQLite 文件和过新版本库验证迁移或拒绝行为，失败保留文件。
- [x] 3.2 为桌面与 CLI 增加按数据目录规范化的 OS 级独占锁，取得锁后才打开存储并恢复；用两个独立进程验证第二进程不能改写活动任务，杀死测试进程后可重新取得锁。
- [x] 3.3 固化两个修复动作与严格请求校验，保留后端和数据库约束；测试未知动作、未知字段、无效 UUID、任意路径/参数请求均被拒绝且无额外任务或执行副作用。
- [x] 3.4 完善持久化任务状态及终态/事件原子性；在临时数据库注入执行失败和终态保存失败，验证可保存错误进入失败，不可保存结果通过错误通道显示待核实，CLI 返回正确非零退出码。
- [x] 3.5 完善请求去重和恢复，保留新任务与重试的区分；验证重复提交、异动作同号冲突、重启后重复请求和遗留任务中断且不重放。
- [x] 3.6 验证数据库检查的只读诊断效果与索引重建的保留性；在含设备、偏好、任务/事件样例的临时库运行后，对比业务记录并核对新增修复结果。
- [x] 3.7 实现一致性升级前备份和不兼容版本保护；用存在 WAL 写入的测试库验证备份可完整恢复，并确认迁移失败和降级不静默生成空库。

## 4. Tailscale 设备目录

- [x] 4.1 将 CLI 读取改为带超时和输出限额的固定参数执行；用受控测试进程或适配层验证超时、超大输出、非零退出、格式错误均可结束并产生明确状态。
- [x] 4.2 补全设备身份和平台状态解析样例；测试本机、同名对端、缺失在线字段、未知字段和缺失身份，保证节点不会被合并或被推测为在线。
- [x] 4.3 调整缓存 IPC 和设备偏好写入以校验当前有效上下文；验证账户切换、启动未登录、刷新失败和节点消失时旧数据不进入当前在线结果，旧选择被正确清除或标为历史。
- [ ] 4.4 完善别名、收藏、搜索与筛选的持久交互；在重启场景验证同一节点偏好恢复、不同网络互不污染、同名节点可分别选择。
- [ ] 4.5 完成可重复进入的新增设备引导与未授权详情提示；在实际可用的测试设备加入网络后验证重新发现，并验证未加入/未可见时提示不伪造成功。
- [ ] 4.6 在 Windows、macOS 两类架构和 Linux 的目标安装形态验证官方 CLI 定位及状态读取；分别记录实测版本、权限与限制，未具备环境的项目保留未完成。

## 5. 简约科技 UI 实装

- [ ] 5.1 按概念图建立颜色、文字、间距、焦点及动效变量和基础组件；检查 1440px/1024px 两种窗口效果与正文 4.5:1 对比度，保持图片只作参考。
- [ ] 5.2 拆分并实现导航、设备列表、详情面板/小窗口抽屉及添加设备对话框；验证键盘选择、焦点返回、长中文名称和“在线/未知/历史”显示。
- [ ] 5.3 实现任务分页、结果与阶段事件详情、最近本机任务区域；使用受控任务验证排队/执行/成功/失败/中断状态，确认无假进度百分比且目标明确为本机。
- [x] 5.4 实现白名单本机修复表单，并分离提交接受与详情读取错误；模拟响应丢失和提交成功但详情失败，确认重试不会新建重复任务，明确重新运行使用新编号。
- [ ] 5.5 实现设置页和刷新间隔保存/恢复、隐藏页面暂停设备刷新；模拟保存失败验证旧有效设置保留且没有虚假成功提示。
- [x] 5.6 补齐各页加载、空列表、无匹配、错误、缓存及权限未开放状态；浏览器无 Tauri 环境下核对预览说明和禁用操作，不把概念图示例设备注入生产数据。
- [ ] 5.7 完成键盘导航、可访问名称、对话框焦点、减少动态效果和小窗口检查；保存实际实现截图，与概念图对照并记录必要差异。

## 6. 集成验证

- [ ] 6.1 完成一次全项目验证：`npm run check`、`npm test`、`npm run build`、`cargo fmt --all --check`、`cargo test --workspace --locked`；记录结果，失败项不得计为通过。
- [ ] 6.2 在真实 Windows Tauri 窗口使用独立测试数据目录验证设备读取、偏好保存、本机修复、任务详情、关闭重启及单实例行为；记录运行结果，不以浏览器预览代替桌面验证。
- [ ] 6.3 对跨模块故障执行回归：状态超时、上下文切换、非法 IPC、存储写入失败及进程中断；逐项核对 specs 场景并确保没有远程执行入口。
- [ ] 6.4 记录能力对照表，区分基础里程碑已验证功能与原 PRD 尚未实现的远程功能；审阅运行文案与发布说明一致。

## 7. 安装包与平台交付

- [x] 7.1 明确 Windows NSIS bundle 和 currentUser 安装设置，运行 `npm run tauri build -- --bundles nsis`；确认实际生成可识别的 `-setup.exe` 并记录 WebView2 安装策略。
- [ ] 7.2 在 Windows 测试环境验证全新安装、首次启动、覆盖安装保留数据及卸载入口；记录 OS、架构、版本和真实结果，避免覆盖用户现有应用数据。
- [ ] 7.3 添加三平台 CI 检查和打包矩阵，使用锁文件、限定发布权限与受信任发布触发；验证 Windows、macOS arm64/x64、Linux 作业各自产出预期格式，不能仅检查 YAML 文件存在。
- [ ] 7.4 在对应 macOS 环境构建并启动两个架构的 DMG 应用；保存安装/启动与数据持久化记录，缺少环境时如实保留未验证状态。
- [ ] 7.5 在 Ubuntu 基线环境构建并验证 AppImage 和 deb 安装/启动；记录系统依赖、架构及持久化结果，不泛称所有 Linux 均支持。
- [ ] 7.6 接入可用的签名/公证材料，或明确标记无签名测试构建；对每个实际产物生成 SHA-256、版本与架构清单，并验证摘要可复算。

## 8. GitHub 仓库和交付记录

- [ ] 8.1 补足 `work/`、工具链及其他生成产物的忽略规则并准备源码提交；检查暂存清单不含数据库、真实设备数据、日志、凭证和 npm 缓存，锁文件及设计规范仍被包含。
- [ ] 8.2 使用已认证账户确认 owner 并创建新的公开 `tailscale-ui` 仓库，关联 remote 后推送已审核源码；通过远程仓库文件和提交记录验证成功，同名冲突或缺少登录时如实报告。
- [ ] 8.3 在授权范围内将实施事项链接到本仓库 GitHub Issues，内容引用 OpenSpec 变更并避免复制过时规范；通过 Issue 链接与内容验证正确归属，无凭证时仅保留待办。
- [ ] 8.4 上传已验证平台的测试发布资产与变更说明、SHA-256 清单；从发布页核对名称、版本、下载资产和各平台验证/签名状态，未交付的平台明确列出。
- [ ] 8.5 整理最终交付入口、运行/打包说明和验证记录，按四份规格逐项复核；所有必需任务确有证据后才宣告本基础里程碑完成，远程产品目标仍单独跟踪。
